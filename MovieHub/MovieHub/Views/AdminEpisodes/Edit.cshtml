@model MovieHub.Models.Episode

@{
    ViewData["Title"] = "Sửa tập phim";
}

<div class="container py-5">

    <div class="row justify-content-center">

        <div class="col-md-8">

            <div class="card shadow">

                <div class="card-body p-4">

                    <h2 class="mb-4">
                        ✏️ Sửa tập phim
                    </h2>

                    @if (ViewBag.Error != null)
                    {
                        <div class="alert alert-danger">
                            @ViewBag.Error
                        </div>
                    }

                    <form method="post"
                          asp-controller="AdminEpisodes"
                          asp-action="Edit">

                        @Html.AntiForgeryToken()

                        <input type="hidden"
                               asp-for="Id" />

                        <div class="mb-3">

                            <label class="form-label">
                                Phim
                            </label>

                            <select asp-for="MovieId"
                                    class="form-select"
                                    required>

                                @foreach (var movie in ViewBag.Movies)
                                {
                                    <option value="@movie.Id">
                                        @movie.Title
                                    </option>
                                }

                            </select>

                        </div>

                        <div class="mb-3">

                            <label class="form-label">
                                Số tập
                            </label>

                            <input asp-for="EpisodeNumber"
                                   type="number"
                                   class="form-control"
                                   min="1"
                                   required />

                        </div>

                        <div class="mb-3">

                            <label class="form-label">
                                Tên tập
                            </label>

                            <input asp-for="Title"
                                   class="form-control"
                                   placeholder="Ví dụ: Tập 1 - Khởi đầu" />

                        </div>

                        <<div class="mb-4">

                            <label class="form-label">
                                Video URL
                            </label>

                            <input asp-for="VideoUrl"
                                   class="form-control"
                                   type="url"
                                   placeholder="https://example.com/video.mp4" />

                            <div class="form-text">
                                Nhập đường dẫn trực tiếp đến file video MP4.
                            </div>

                        </div>

                        <div class="d-flex gap-2">

                            <button type="submit"
                                    class="btn btn-primary">
                                💾 Lưu thay đổi
                            </button>

                            <a asp-controller="AdminEpisodes"
                               asp-action="Index"
                               class="btn btn-secondary">
                                Hủy
                            </a>

                        </div>

                    </form>

                </div>

            </div>

        </div>

    </div>

</div>